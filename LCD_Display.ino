#include "Display.h"

void setup()
{
  pinMode(RS,OUTPUT);
  pinMode(E,OUTPUT);

  pinMode(DB0,OUTPUT);
  pinMode(DB1,OUTPUT);
  pinMode(DB2,OUTPUT);
  pinMode(DB3,OUTPUT);
  pinMode(DB4,OUTPUT);
  pinMode(DB5,OUTPUT);
  pinMode(DB6,OUTPUT);
  pinMode(DB7,OUTPUT);
  
}

void loop()
{
  // Clear_Display();
  // Set_Entry_Mode(1,1);
  // Set_Display(1,1,0);
}
